substitutions:
  friendly_name: Voice PE Arbeitszimmer

packages:
  home-assistant-voice: !include home-assistant-voice.yaml

esphome:
  name: ha-voice-099f06
  friendly_name: ${friendly_name}
  platformio_options:
    extra_scripts:
      # FC patch DEACTIVATED — testing if esp-tflite-micro 1.3.4's
      # "Use ESP-NN optimisations for FullyConnected Per-Channel" fix
      # combined with esp-nn 1.2.3 makes the workaround unnecessary.
      # - pre:../../../patch_esp_nn_fc.py
      - pre:../../../patch_esp_tflite_micro_version.py
      - pre:../../../patch_esp_nn_version.py

api:
  encryption:
    key: Qy0E0XoeaM+8hhsUFCunFYvDTTzuRcOvYc5ehQXFa/A=

wifi:
  ssid: !secret wifi_ssid
  password: !secret wifi_password
