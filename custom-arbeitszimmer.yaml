substitutions:
  friendly_name: Voice PE Arbeitszimmer

packages:
  home-assistant-voice: !include home-assistant-voice.yaml

esphome:
  name: ha-voice-099f06
  friendly_name: ${friendly_name}
  platformio_options:
    extra_scripts:
      # Phase 3.2 full-stack test on bcresnet-test:
      # Both ESP-NN patches DEACTIVATED — testing whether the upstream fixes
      # in esp-tflite-micro 1.3.4 + esp-nn 1.2.3 make both workarounds
      # unnecessary, also for the BC-ResNet Conv2D path that previously
      # crashed at esp_nn/conv.cc:291.
      # - pre:../../../patch_esp_nn_fc.py
      # - pre:../../../patch_esp_nn_conv.py
      - pre:../../../patch_esp_tflite_micro_version.py
      - pre:../../../patch_esp_nn_version.py

api:
  encryption:
    key: Qy0E0XoeaM+8hhsUFCunFYvDTTzuRcOvYc5ehQXFa/A=

wifi:
  ssid: !secret wifi_ssid
  password: !secret wifi_password
